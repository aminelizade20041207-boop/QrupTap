import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.QrupTap.app',
  appName: 'QrupTap',
  webDir: 'out',
  server: {
    androidScheme: 'https'
  },
};

export default config;
